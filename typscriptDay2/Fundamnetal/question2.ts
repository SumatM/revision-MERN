// **Question 2:**
// Now, let's move on to functions. Create a TypeScript function called `calculateTotal` that takes in two parameters: `price` (a number) and `quantity` (a number). The function should return the total cost, which is the product of the `price` and `quantity`.

function calculateTotal(price: number, quantity: number): number {
  return price * quantity;
}
