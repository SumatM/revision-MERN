// **Question 1:**
// Create a TypeScript interface called `Person` with the following properties:
// - `name`: a string
// - `age`: a number
// - `email`: a string

// Then, declare a variable `person` of type `Person` and initialize it with some sample data.

interface Person {
  name: string;
  age: number;
  email: string;
}

let person: Person = {
  name: "aman",
  age: 18,
  email: "a@email.com",
};
